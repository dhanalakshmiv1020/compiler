%{
#include <stdio.h>
#include <string.h>

int max = 0;
char longest[100];
%}

%%
[A-Za-z]+  {
                if (yyleng > max)
                {
                    max = yyleng;
                    strcpy(longest, yytext);
                }
            }

[ \t\n]+   ;
.          ;
%%

int yywrap()
{
    return 1;
}

int main()
{
    printf("Enter sentence (Press Ctrl+Z then Enter to stop):\n");
    yylex();
    printf("\nLongest Word : %s\n", longest);
    printf("Length       : %d\n", max);
    return 0;
}